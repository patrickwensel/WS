@model int

@(Html.Kendo().DropDownListFor(m => m)
                  .OptionLabel("Select Category...")
                  .DataTextField("Name")
                  .DataValueField("ID")
                  .DataSource(dataSource => dataSource.Read(read => read.Action("ReadActiveProductCategories", "Admin")))
            )
