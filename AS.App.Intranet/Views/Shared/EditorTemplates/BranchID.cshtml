@model decimal?

@(Html.Kendo().DropDownListFor(m => m)
                  .AutoBind(false)
                  .OptionLabel(" ")
                  .DataTextField("Name")
                  .DataValueField("ID")
                  .HtmlAttributes(new { data_value_primitive = true })
                  .DataSource(dataSource => dataSource.Read(read => read.Action("GetBranchsFilteredByBusinessUnitID", "Hierarchy").Data("filterBranchs")).ServerFiltering(true))
                  .CascadeFrom("BusinessUnitID")
                  .Events(e => e.Change("branchChange"))
            )
@Html.ValidationMessageFor(m => m)

