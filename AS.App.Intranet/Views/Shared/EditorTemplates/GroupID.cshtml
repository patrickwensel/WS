@model decimal

@(Html.Kendo().DropDownListFor(m => m)
                  .AutoBind(false)
                  .OptionLabel(" ")
                  .DataTextField("Name")
                  .DataValueField("ID")
                  .DataSource(dataSource => dataSource.Read(read => read.Action("GetGroups", "Video").Data("filterGroup")).ServerFiltering(true))
                  .CascadeFrom("AreaID")
            )
@Html.ValidationMessageFor(m => m)